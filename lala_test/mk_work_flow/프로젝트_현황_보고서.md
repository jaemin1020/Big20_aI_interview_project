# A4 Project 현황 분석 및 향후 진행 가이드
**기준 일자**: 2026-02-07
**수정 사항**: 사용자 피드백 반영 (기능 동작 여부 기준 재평가)

---

## 1. 개요 (Summary)
**🚨 중요**: 현재 프로젝트는 주요 모듈(UI, AI Worker 등)의 **기본 코드(Scaffolding/Prototype)**만 작성된 상태이며, **실제 기능 동작 및 연동은 검증되지 않았습니다.**
따라서 "완료"라는 표현 대신 **"코드 초안 작성됨 (Not Verified)"**으로 상태를 정정합니다.

---

## 2. 진행 상황 (Status Check - Revised)

### ⚠️ 개발 진행 중 / 검증 필요 (Code Exists but Not Verified)
*파일은 존재하나, 실제 작동 여부 및 모듈 간 연결이 확인되지 않음.*

| Task ID | Task Name | Status | Current Context |
| :--- | :--- | :--- | :--- |
| **2.4.3** | Frontend UI | **작성됨 (미연동)** | `frontend/src/` 내 페이지 코드는 있으나 API 연결 및 실제 데이터 렌더링 검증 안 됨. |
| **2.4.6** | 감정분석(Vision) | **초안 작성됨** | `ai-worker/tasks/vision.py` 로직 존재. 단, 영상 스트림 수신 및 결과 DB 저장 흐름 미검증. |
| **2.3.6** | LLM 질문생성 | **초안 작성됨** | `ai-worker/tasks/question_generator.py` 존재. 실제 LLM 연동 및 응답 품질 테스트 전. |
| **2.4.1** | 시스템 DB | **스키마 생성됨** | `models.py` 존재. 실제 데이터 적재 및 조회 쿼리 동작 여부 확인 필요. |

### ❌ 미구현 / 설계 미흡 (Not Implemented / Missing Design)
*코드조차 없거나, 구체적인 설계가 누락된 항목.*

| Task ID | Task Name | Criticality | Details |
| :--- | :--- | :--- | :--- |
| **2.3.7** | **RAG 파이프라인** | **High** | 문서 로더(`rag_loader.py`), 벡터 검색 로직, Context 주입부 미구현. |
| **2.4.7** | **시스템 통합** | **Critical** | `Frontend` <-> `Backend` <-> `AI Worker` 간 데이터 흐름(End-to-End) 연결 안 됨. |
| **2.3.x** | **STT / TTS 모델** | **High** | 구체적인 모델 선정 및 파이프라인(Streaming vs Batch) 설계와 구현체 없음. |
| **2.3.x** | **LLM 아키텍처** | **High** | 단순 질문 생성을 넘어선, 전체 대화 관리(History, Context) 아키텍처 설계 부재. |

---

## 3. 향후 할 일 (Action Items)

### 🚀 1. 기능 검증 (Verification)
기존에 작성된 "초안 코드"들이 실제로 작동하는지 개별 단위 테스트를 수행해야 합니다.
- `ai-worker`: `vision.py`, `question_generator.py` 단독 실행 테스트.
- `frontend`: API Mocking을 통한 UI 렌더링 확인.

### 📜 2. 상세 설계 (Design) - [Work Flow 참조]
작성이 안 된 모듈(STT, TTS, LLM 상세, RAG)에 대한 설계를 먼저 확정해야 합니다.
- `잔여_과제_구현_워크플로우.md` 워크플로우를 따라 설계를 진행합니다.

### 🛠️ 3. 시스템 통합 (Integration)

---
**작성자**: 함유라
**작성일**: 2026-02-07
