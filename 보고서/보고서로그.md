2026-02-06 :

1. 이력서를 마크다운으로 변환후 -> json구조 바꾸고 -> 임베딩 하는게 맞다고 생각함

2.이미깔린 엑사원으로 json변환,질문생성 한버에 쓰기로 

3.마크다운,엑사원json변환까지 약 3분정도 걸림

4.exaone3.5:latest ->exaone3.5:2.4b 속도빠르게 하기 위해 다른 버전으로 바꿈

5 엑사원이 말을 안듦음 계속 요약본을 줌

원인:EXAONE 2.4B가 “요약 금지”라고 아무리 써도 **의미 기반 재서술(paraphrasing)**을 해버리는 게 정상 동작이야

6. 코드 수정함

👉 **LLM에게 ‘추출(extraction)’을 시키지 말고, ‘위치 지정(indexing)’만 시키고**

👉 **원문 복사는 Python 코드가 하도록 분리해야 함**

7. 원문그대로 출력이니 llm사용 안

* 참고사

[수집/변환 계층]   ← LLM 금지 영역
PDF → Parser → Markdown → JSON → DB

[지능 계층]        ← LLM 영역
RAG / QA / 분석 / 평가 / 생성 / 추천 / 면접
