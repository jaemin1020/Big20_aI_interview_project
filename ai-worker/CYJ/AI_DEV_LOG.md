# 🛡️ AI 면접 시스템 개발 및 트러블슈팅 일지 (Dev Log)

**작성일**: 2026년 2월 5일 ~ 현재  
**작성자**: Project Big20 AI Team  
**문서 설명**: 본 문서는 AI 면접 시스템 개발 과정에서 발생한 주요 에러와 이슈, 그리고 해결 과정을 단계별로 상세히 기록한 **기술 회고록(Retrospective)**입니다.

---

## 1. 🏗️ 전체 시스템 개발 단계 (Milestones)

| 단계 | 파트명 | 주요 목표 | 상태 |
| :--- | :--- | :--- | :--- |
| **Phase 1** | 초기 환경 구축 | Python 환경 설정 및 필수 라이브러리(`mediapipe`, `opencv`) 설치 | ✅ 완료 |
| **Phase 2** | 핵심 엔진 구현 | 얼굴 랜드마크 추출 및 실시간 시선/자세 추적 로직 개발 | ✅ 완료 |
| **Phase 3** | UI/UX 고도화 | 한국어 폰트 적용 및 사용자 친화적 대시보드(영점 조절) 구현 | ✅ 완료 |
| **Phase 4** | 정밀 검증 (Tuning) | 감정 인식(슬픔/놀람) 민감도 최적화 및 평가 로직 확정 | ✅ 완료 |
| **Phase 5** | **시스템 통합 (Integration)** | **TTS/STT/Vision/LLM 모듈 연결 및 파이프라인 흐름 검증** | **🔄 진행 중** |
| **Final** | 결과 리포트 | JSON 데이터 저장 및 종합 성적표 산출 기능 탑재 | ✅ 완료 |

---

## 2. 🚨 에러 및 이슈 히스토리 (Troubleshooting)

### 🔴 Issue #1: MediaPipe 모델 파일 누락
*   **발생 위치**: [Phase 1] 초기 실행 단계 > 모델 로드(`vision.FaceLandmarker.create_from_options`)
*   **증상/에러**: `RuntimeError: Unable to open file: face_landmarker.task`
*   **원인 분석**: 코드 상에서는 `model_path = 'face_landmarker.task'`로 파일을 찾고 있었으나, 실제로 해당 경로에 구글의 학습된 모델 파일이 다운로드되어 있지 않았음.
*   **해결 방법**: 터미널에서 `curl` 명령어를 사용하여 구글 서버로부터 모델 파일을 직접 다운로드함.
    ```powershell
    curl -L -o face_landmarker.task https://storage.googleapis.com/...
    ```

### 🔴 Issue #2: IndentationError (들여쓰기 오류)
*   **발생 위치**: [Phase 2] 코드 수정 단계 > `replace_file_content` 툴 사용 후
*   **증상/에러**: 
    ```python
    File "CV-V2-TASK.py", line 66
        landmarks = result.face_landmarks[0]
    IndentationError: unexpected indent
    ```
*   **원인 분석**: AI가 코드를 수정하는 과정에서 `if result.face_landmarks:` 조건문 아래의 코드를 붙여넣을 때, 파이썬의 문법 규칙인 들여쓰기(Space 4칸)가 어긋남.
*   **해결 방법**: 코드를 전면 재작성(`write_to_file`)하여 들여쓰기 레벨을 완벽하게 맞춤.

### 🔴 Issue #3: 감정 인식 민감도 저하 (슬픔/놀람 인식 불가)
*   **발생 위치**: [Phase 4] 정밀 검증 단계 > `CV_EMOTION_VERIFIER.py` 테스트 중
*   **증상**: 사용자가 의도적으로 슬픈 표정을 지어도 AI가 이를 감지하지 못하고 계속 '평온(Neutral)'으로만 판정함. 행복(Happy)은 잘 인식됨.
*   **원인 분석**: 
    1.  슬픔(`mouthFrown`)의 랜드마크 변화량이 행복(`mouthSmile`)에 비해 상대적으로 미미함.
    2.  판정 문턱값(Threshold)이 0.25로 너무 높게 설정되어 있었음.
*   **해결 방법**: '밸런스 튜닝' 적용
    1.  **가중치 증폭**: `sad_score` 산출 시 입력값에 **1.8배**를 곱함.
    2.  **임계값 하향**: 문턱값을 0.25 → **0.12**로 대폭 낮춤. -> **[해결 완료]**

### 🔴 Issue #4: DeepFace 모델의 실시간성 한계
*   **발생 위치**: [Phase 1] 기획 및 초기 테스트 단계
*   **증상**: `DeepFace` 라이브러리를 사용했을 때 프레임 처리 속도가 0.5초 이상 지연되어 화면이 뚝뚝 끊김(Lag).
*   **원인 분석**: DeepFace의 VGG-Face 모델은 무거운 연산을 수행하므로 실시간(30FPS) 추론에는 부적합함.
*   **해결 방법 (아키텍처 변경)**:
    - **Before**: DeepFace + MediaPipe 동시 사용
    - **After**: **하이브리드 아키텍처** 도입
        - 입장 시(1회): DeepFace (본인 인증, 느려도 됨)
        - 면접 중(실시간): MediaPipe (태도 분석, 매우 빠름)

---

### 🔴 Issue #5: [TTS 통합] 'qwen_tts' 라이브러리 부재로 인한 Import Error
*   **발생 위치**: [Phase 5] 시스템 통합 단계 > `CYJ/main_test.py` 실행 시
*   **에러 메시지**: `ModuleNotFoundError: No module named 'qwen_tts'`
*   **발생 상황**: 온프레미스 AI 워커 컨테이너 내부에서 TTS 모델(`Qwen3-TTS-12Hz-0.6B-CustomVoice`)을 호출하려 했으나, 해당 파이썬 패키지가 설치되어 있지 않음이 발견됨.
*   **원인 분석**: 
    1.  이전 작업 세션에서 `qwen_tts`를 임시로 설치했으나, 도커 이미지를 리빌드하거나 컨테이너를 재생성하는 과정에서 휘발되었을 가능성.
    2.  또는 로컬 환경과 도커 환경의 혼동으로 도커 내부에 실제로는 설치되지 않았던 상태.
*   **임시 조치**: **Mock-Up 전략 사용**
    *   라이브러리가 없을 경우 에러를 뱉고 죽는 대신, `WARNING` 로그를 출력하고 **가짜 오디오(Sine Wave Beep)**를 생성하도록 `tts_service.py` 로직 수정.
    *   이를 통해 파이프라인의 뒷단(파일 생성 확인, API 응답 등) 검증을 멈춤 없이 진행함.
*   **해결 방법**: `docker exec`를 통해 컨테이너 내부에 `pip install qwen-tts` 직접 실행 완료. ✅

### 🔴 Issue #6: [TTS 코드] `NameError: name 'torch' is not defined`
*   **발생 위치**: `CYJ/tts_service.py` 내 `get_tts_model` 함수
*   **증상**: 모델 로딩 중 `torch.bfloat16` 호출 시 `torch` 모듈이 정의되지 않았다는 에러 발생.
*   **원인**: 파일 상단에 `import torch`가 누락됨.
*   **해결**: `import torch` 및 `import sys` 추가 완료. ✅

### 🟡 Issue #7: [시스템 환경] `sox` 및 `flash-attn` 부재 (경고)
*   **증상**: `qwen-tts` 실행 시 `SoX could not be found!` 및 `flash-attn is not installed` 경고 출력.
*   **영향**: `sox`는 오디오 처리에 필요할 수 있으며, `flash-attn`은 추론 속도 최적화에 사용됨.
*   **현재 상태**: 경고만 출력될 뿐, **TTS 음성 생성은 정상 작동함**. (PyTorch 전용 모드 사용)
*   **향후 조치**: 필요 시 `apt-get install sox` 실행 예정.

---

### 🔴 Issue #11: [통합 테스트] EXAONE GGUF 모델 파일 누락
- **발생 시각**: 2026-02-08 16:40
- **발생 위치**: `CYJ/main_integrated_test.py` 실행 시 → `utils/exaone_llm.py`
- **에러 메시지**: 
  ```
  FileNotFoundError: 모델 파일을 찾을 수 없습니다: /app/models/EXAONE-3.5-7.8B-Instruct-Q4_K_M.gguf
  ```
- **발생 상황**: 전체 파이프라인(STT→TTS→LLM→CV) 통합 테스트를 실행하려고 했으나, LLM 모델 파일이 Docker 컨테이너에 없음
- **원인 분석**: 
  1. Docker 컨테이너 `/app/models/` 폴더에 EXAONE GGUF 모델 파일이 없음
  2. 호스트 시스템에도 `.gguf` 파일이 검색되지 않음
  3. 다른 팀원이 이전에 테스트했던 모델이지만, 현재 환경에는 다운로드되지 않은 상태
  4. Docker 볼륨 마운트 설정 또는 모델 다운로드 스크립트가 누락되었을 가능성
- **해결 방법**: 
  1. Hugging Face에서 EXAONE-3.5-7.8B-Instruct GGUF 모델 다운로드
  2. Docker 컨테이너 내부 `/app/models/` 경로에 배치
  3. 또는 Docker Compose에서 호스트 모델 폴더를 마운트
- **재발 방지**: 
  - 모델 다운로드 스크립트 작성 및 문서화
  - Docker Compose에 모델 볼륨 마운트 설정 추가
  - `.gitignore`에 모델 파일 제외, README에 다운로드 가이드 추가
- **상태**: ✅ 해결 (모델 다운로드 완료 - 4.44GB)

---

### 🔴 Issue #12: [통합 테스트] llama.cpp가 EXAONE 아키텍처 미지원
- **발생 시각**: 2026-02-08 16:54
- **발생 위치**: `CYJ/main_integrated_test.py` 실행 시 → `llama-cpp-python` 라이브러리
- **에러 메시지**: 
  ```
  llama_model_load: error loading model: error loading model architecture: unknown model architecture: 'exaone'
  ValueError: Failed to load model from file: /app/models/EXAONE-3.5-7.8B-Instruct-Q4_K_M.gguf
  ```
- **발생 상황**: 
  - EXAONE GGUF 모델 다운로드는 성공 (4.44GB)
  - 하지만 `llama-cpp-python`이 모델을 로드하려 할 때 "unknown model architecture: 'exaone'" 오류 발생
- **원인 분석**: 
  1. 설치된 `llama-cpp-python` 버전이 EXAONE 아키텍처를 지원하지 않음
  2. EXAONE은 비교적 최근 모델이라 구버전 llama.cpp에서 지원하지 않을 수 있음
  3. 또는 EXAONE GGUF 변환이 표준 llama.cpp와 호환되지 않는 방식으로 변환되었을 가능성
- **해결 방법**: 
  - **옵션 1**: `llama-cpp-python` 최신 버전으로 업그레이드
  - **옵션 2**: EXAONE 공식 지원 버전의 llama.cpp 사용
  - **옵션 3**: transformers 라이브러리로 원본 PyTorch 모델 사용 (GGUF 대신)
  - **선택**: 옵션 3 - transformers로 원본 EXAONE 모델 사용 (이미 설치됨)
- **재발 방지**: 
  - 모델 포맷 호환성 사전 확인
  - 공식 지원 모델 포맷 우선 사용
- **상태**: ✅ 해결 (llama-cpp-python 0.3.16 업그레이드 완료, EXAONE 모델 로드 성공)

---

### 🔴 Issue #13: [통합 테스트] stt_poc 모듈 경로 오류
- **발생 시각**: 2026-02-08 16:59
- **발생 위치**: `CYJ/main_integrated_test.py` 43번째 줄
- **에러 메시지**: 
  ```
  ModuleNotFoundError: No module named 'stt_poc'
  ```
- **발생 상황**: 
  - LLM 모델 로드는 성공
  - TTS 모듈(`stt_poc.tts_qwen3`) 임포트 시도 시 오류 발생
- **원인 분석**: 
  - CYJ 폴더에서 실행하는 파일이 `poc/stt_poc` 폴더를 찾지 못함
  - Python path 설정 문제 또는 상대 경로 사용 필요
- **해결 방법**: 
  - 통합 테스트 코드의 경로 설정 수정
  - `sys.path.append("/app/poc")` 추가
- **재발 방지**: 
  - 프로젝트 구조 정리 및 모듈 경로 표준화
- **상태**: ✅ 해결 (경로 수정으로 모든 모듈 로드 성공)

---

### 🔴 Issue #14: [통합 테스트] Vision 모듈 DB 스키마 오류
- **발생 시각**: 2026-02-08 17:04
- **발생 위치**: `tasks/vision.py` → PostgreSQL 데이터베이스
- **에러 메시지**: 
  ```
  psycopg.errors.UndefinedColumn: column interviews.company_id does not exist
  ```
- **발생 상황**: 
  - Vision 모듈 연결은 성공
  - 감정 분석 및 시선 추적 호출 시 DB 조회 실패
- **원인 분석**: 
  - `interviews` 테이블에 `company_id` 컬럼이 없음
  - 데이터베이스 마이그레이션이 완료되지 않았거나 스키마가 코드와 불일치
- **해결 방법**: 
  - 데이터베이스 마이그레이션 실행 필요
  - 또는 Vision 코드에서 `company_id` 참조 제거
- **재발 방지**: 
  - DB 스키마 변경 시 마이그레이션 스크립트 작성
- **상태**: ⚠️ 부분 해결 (Vision 모듈 연결은 성공, DB 문제는 별도 처리 필요)

---

## 🎊 통합 테스트 결과 요약 (2026-02-08 17:11 - Supertonic 2 사용)

### ✅ 성공한 기능
1. **LLM 질문 생성** (EXAONE-3.5-7.8B)
   - 소요 시간: 15.43초
   - 생성된 질문: "파이썬 백엔드 개발자 직무에 지원하게 된 구체적인 동기는 무엇인가요?"
   
2. **TTS 음성 합성** (Supertonic-2-Korean) ⭐ **성능 대폭 개선**
   - 소요 시간: **0.71초** (이전 Qwen3 21초 → **30배 빠름**!)
   - 출력 파일: `/app/stt_poc/outputs/integrated_test_question.wav`
   - 모델 로딩: 1.40초 (Qwen3 14초 → **10배 빠름**)
   
3. **STT 답변 인식** (Whisper-large-v3-turbo - Mock으로 표기)
   - Mock 답변 사용으로 테스트 통과
   - 실제 Whisper 연동은 추후 구현 예정
   
4. **LLM 답변 평가** (EXAONE-3.5-7.8B)
   - 소요 시간: 41.65초
   - 기술 점수: 2/5 (질문과 답변의 연관성 분석 완료)
   - 피드백 생성 완료
   
5. **Vision 분석 모듈 연결**
   - DeepFace 모델 초기화 성공
   - 감정 분석, 시선 추적 함수 호출 성공 (DB 오류 있지만 모듈 자체는 작동)

### ⚠️ 부분적 문제
- Vision 모듈: DB 스키마 불일치로 데이터 저장 실패 (모듈 연결은 성공)

### 📊 성능 측정 (Supertonic 2 사용)
| 항목 | Qwen3-TTS | Supertonic-2 | 개선 |
|------|-----------|--------------|------|
| TTS 모델 로딩 | 14.57초 | 1.40초 | **10배 빠름** ⚡ |
| TTS 음성 생성 | 21.19초 | 0.71초 | **30배 빠름** ⚡⚡⚡ |
| 전체 파이프라인 | ~77초 | ~58초 | **25% 단축** |

### 🎯 최종 선택
- **TTS 모델**: **Supertonic-2-Korean** (한국어 공식 지원, 압도적 성능)
- **STT 모델**: **Whisper-large-v3-turbo** (실제 연동 예정)
- **LLM 모델**: **EXAONE-3.5-7.8B-Instruct** (한국어 최적화)



---

## 3. 📝 유지보수 가이드 (Maintenance Guide)

### 💡 특정 감정이 너무 잘 뜨거나 안 뜰 때
*   **수정 파일**: `CV-V2-TASK.py` 내 `[Step 7] 감정 분석` 파트
*   **방법**: `if brow_down > 0.35:` 부분의 숫자를 조절하세요.
    - 숫자를 **낮추면** 더 예민해집니다. (0.35 -> 0.2)
    - 숫자를 **높이면** 더 둔해집니다. (0.35 -> 0.5)

### 💡 리포트 점수 배점 변경
*   **수정 파일**: `CV-V2-TASK.py` 상단 `[Step 1] 사용자 설정` 파트
*   **방법**: `WEIGHT_CONFIDENCE` 등의 변수 값을 수정하세요. (단, 총합은 1.0이 되도록 권장)

### 💡 실제 목소리 재생
*   이제 라이브러리 설치와 코드 수정이 완료되어 Mock 모드 대신 **실제 음성 생성이 가능**합니다.
*   생성된 파일 경로: `/app/CYJ/outputs/test_interview_XXXXX.wav`

---

## 4. 🔴 2026-02-09 시스템 통합 및 웹 테스트 오류 로그

### Issue #7: 회원가입 API 404 에러
*   **발생 시간**: 2026-02-09 10:15
*   **파트**: 백엔드 API - auth.py
*   **증상**: 프론트엔드에서 회원가입 버튼 클릭 시 반응 없음, `POST /register - 404 Not Found`
*   **원인**: 백엔드 auth 라우터에 `/auth` prefix 설정으로 경로 불일치
    - 프론트엔드: `/register` 호출
    - 백엔드: `/auth/register`로 라우팅됨
*   **해결**: `backend-core/routes/auth.py` 14번째 줄
    ```python
    router = APIRouter(prefix="", tags=["auth"])  # /auth 제거
    ```

### Issue #8: 이력서 업로드 API 404 에러
*   **발생 시간**: 2026-02-09 10:30
*   **파트**: 백엔드 API - resumes.py
*   **증상**: `POST /resumes/upload - 404 Not Found`
*   **원인**: resumes 라우터에 `/api` prefix 불일치
*   **해결**: `backend-core/routes/resumes.py` 18번째 줄
    ```python
    router = APIRouter(prefix="/resumes", tags=["Resumes"])  # /api 제거
    ```

### Issue #9: 이력서 상태 조회 422 에러
*   **발생 시간**: 2026-02-09 10:40
*   **파트**: 프론트엔드 - ResumePage.jsx
*   **증상**: `GET /resumes/undefined - 422 Unprocessable Entity`
*   **원인**: 백엔드 응답 필드명 불일치
    - 백엔드: `{resume_id: 3}`
    - 프론트엔드: `uploadData.id` → undefined
*   **해결**: `frontend/src/pages/landing/ResumePage.jsx` 29번째 줄
    ```javascript
    const resumeId = uploadData.resume_id;  // 올바른 필드명
    ```

### Issue #10: 이력서 임베딩 생성 UnboundLocalError
*   **발생 시간**: 2026-02-09 10:50
*   **파트**: AI Worker - resume_embedding.py
*   **증상**: `UnboundLocalError: local variable 'engine' referenced before assignment`
*   **원인**: `engine` 변수를 7번째 줄과 87번째 줄에서 중복 import
*   **해결**: `ai-worker/tasks/resume_embedding.py` 87번째 줄
    ```python
    from db import ResumeSectionEmbedding, ResumeSectionType  # engine 제거
    ```

### Issue #11: Whisper STT 환각 현상
*   **발생 시간**: 2026-02-09 11:10
*   **파트**: 프론트엔드 - EnvTestPage.jsx (마이크 테스트)
*   **증상**: 사용자 음성과 다른 텍스트 출력 ("한글자막 제공 및...")
*   **원인**: MediaRecorder가 오디오를 제대로 캡처하지 못해 Whisper에 빈 오디오 전달
*   **해결 시도**:
    1. audioContext.destination 연결 제거 (피드백 방지)
    2. opus 코덱 명시: `mimeType: 'audio/webm;codecs=opus'`
*   **결과**: 부분 개선 (간헐적 단어 인식)

### Issue #12: Whisper STT - webm 파일 형식 오류 ✅
*   **발생 시간**: 2026-02-09 11:19  
*   **파트**: AI Worker - tasks/stt.py  
*   **증상**:
    ```
    ValueError: Soundfile is either not in the correct format or is malformed
    ```
*   **원인**: Whisper Transformers Pipeline이 webm 파일을 읽지 못함
*   **해결**: `ai-worker/tasks/stt.py` (70-85번째 줄)
    ```python
    # pydub를 사용하여 webm → wav 변환
    from pydub import AudioSegment
    audio = AudioSegment.from_file(temp_path, format="webm")
    wav_path = temp_path.replace(".webm", ".wav")
    audio.export(wav_path, format="wav")
    
    # 변환된 wav 파일을 Whisper에 전달
    result = stt_pipeline(wav_path, generate_kwargs={"language": "korean"})
    ```
*   **결과**: ✅ webm → wav 변환 구현 완료

---

## 5. 📊 오류 통계 (2026-02-09)
*   **총 발생 오류**: 6개
*   **완전 해결**: 5개 ✅
*   **테스트 필요**: 1개 (Issue #11)

