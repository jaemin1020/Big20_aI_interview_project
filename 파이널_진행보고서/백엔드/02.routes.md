# ⚙️ BIGTERVIEW 백엔드 라우터 구조 및 핵심 로직 분석

백엔드의 모든 핵심 부서(Router) 코드를 종합적으로 훑어본 결과, 이 프로젝트가 단순한 웹 서버가 아니라 **진짜 실무 수준의 복잡한 비동기 AI 파이프라인**을 갖추고 있음이 확인되었습니다.

개발자분들이 각 부서의 역할을 아주 명확하게 나누어 놓았습니다. 마치 유기적으로 돌아가는 거대한 톱니바퀴와 같습니다. 7개의 파일이 각각 어떤 역할을 맡고 있는지, 그리고 코드 속에 숨겨진 **'아주 똑똑하게 설계된 포인트'**들을 정리했습니다.

---

## 1. 🏢 기본 관리 부서 (`auth.py`, `users.py`)

이 두 곳은 서비스의 근간이 되는 회원 관리팀입니다.

* **`auth.py`**: 회원가입, 로그인(JWT 토큰), 비밀번호 변경, 탈퇴(Soft Delete)를 담당합니다.
* **`users.py`**: 내 프로필 보기 및 수정을 담당합니다.

> **💡 똑똑한 포인트**
> `users.py`에서 희망 직무 같은 배열(Array/JSON) 데이터를 수정할 때 `attributes.flag_modified(db_user, ...)`를 사용한 점이 아주 훌륭합니다. SQLAlchemy는 배열 내부의 변경을 잘 감지하지 못하는데, 이 부분을 명시적으로 짚어준 섬세한 코드입니다.

---

## 2. 🎯 기업 정보 및 검색팀 (`companies.py`)

단순히 DB에서 회사 이름을 꺼내오는 곳이 아닙니다. 벡터 검색(Vector Search) 기술이 들어간 놀라운 부서입니다!

> **💡 똑똑한 포인트**
> `find_similar_companies` 함수를 보면 `Company.embedding.cosine_distance`를 사용합니다. 즉, AI가 텍스트를 숫자로 변환한 임베딩 값을 바탕으로 **"이 회사와 분위기/인재상이 가장 비슷한 회사"**를 수학적으로 계산해서 추천해 주는 강력한 기능이 탑재되어 있습니다.

---

## 3. 📄 서류 처리팀 (`resumes.py`)

이력서 전담 부서입니다. PDF를 받아서 Celery 일꾼(`parse_resume_pdf`)에게 넘기는 역할을 합니다.

> **🚨 체크 포인트**
> 기존 `main.py`에도 똑같이 `/resumes/upload` 코드가 중복으로 존재했습니다. 이 `routes/resumes.py` 코드가 훨씬 깔끔하고 큐(`cpu_queue`)도 정확하게 잡혀 있으므로, `main.py`에 있는 이력서 코드는 지워버리고 이 파일로 완전히 대체(이사)하는 리팩토링이 필요합니다.

---

## 4. 👂 AI의 귀 (`stt.py`)

사용자의 음성을 텍스트로 바꿔주는 곳입니다.

* 사용자가 마이크로 말한 음성 파일을 Base64로 압축해 넘기면, AI 일꾼(`tasks.stt.recognize`)이 텍스트로 변환해 줍니다.
* 원래는 실시간 통신(WebSocket)을 쓰려다가, 안정성을 위해 REST API(POST) 방식으로 바꾼 흔적(Deprecated 주석)이 보입니다. 실무에서 아주 흔하게 일어나는 훌륭한 아키텍처 변경입니다.

---

## 5. 🧠 AI 면접의 심장 (`interviews.py`, `transcripts.py`)

가장 길고, 가장 중요하고, 가장 정교하게 짜인 핵심 엔진룸입니다! 이 두 파일이 협력해서 AI 면접관을 움직입니다.

### `interviews.py` (면접 판 깔아주기)

* 사용자의 이력서를 분석해 직무 전환자(비전공자)인지 판단하고, 그에 맞는 맞춤형 면접 시나리오(Transition vs Standard)를 꺼내옵니다.
* 질문을 생성하자마자 백그라운드로 TTS(Text-To-Speech) 작업을 던져서 오디오 파일을 미리 만들어둡니다. (사용자가 대기하는 시간을 없애는 마법!)
* 면접이 끝나는 순간 AI 평가 리포트 생성(`generate_final_report`) 작업을 지시합니다.

### `transcripts.py` (티키타카와 눈치 게임)

* 사용자가 대답을 서버로 보내면 DB에 기록합니다.

> **💡 천재적인 포인트 (비동기 처리의 예술)**
>
> 1. 사용자 대답이 끝나자마자 **"다음 질문 만들어!"**(`generate_next_question`)라는 명령은 즉시 실행합니다. (빠른 대화 흐름을 위해)
> 2. 그런데 방금 사용자가 한 대답에 대한 **"평가 및 분석"**(`analyze_answer`) 명령은 `countdown=10` 옵션을 주어 10초 뒤에 실행되게 미뤄둡니다.
>
> **왜 그랬을까요?** 무거운 AI(GPU)가 평가를 하느라 다음 질문을 늦게 뱉으면 사용자가 어색함을 느끼기 때문입니다. 진짜 사람처럼 *"일단 다음 질문부터 던지고, 평가는 뒤에서 천천히 하자"*는 완벽한 비동기 설계입니다!
>
