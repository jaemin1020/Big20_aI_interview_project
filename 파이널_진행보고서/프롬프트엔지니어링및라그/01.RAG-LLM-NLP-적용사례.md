# 🤖 RAG/LLM 및 NLP 기술 활용 보고서

---

## 1. 개요
본 보고서는 우리 프로젝트에서 **EXAONE-3.5 LLM**과 **RAG(Retrieval-Augmented Generation)** 기술을 결합하여, 지원자의 이력서에 특화된 고도화된 면접 질문을 생성하는 방식에 대해 기술한다.

---

## 2. 핵심 기술 스택 및 라이브러리 (LLM/NLP)

*   **LLM 엔진**: LG EXAONE-3.5-7.8B-Instruct (GGUF 양자화 모델)
    *   선정 이유: 한국어 성능이 뛰어나며, 로컬 환경에서 추론이 가능한 최적의 파라미터 사이즈를 보유.
*   **프레임워크**: LangChain
    *   `PromptTemplate`, `StrOutputParser` 등을 활용하여 추론 파이프라인을 체인 형태로 구성.
*   **텍스트 정제**: 정규표현식(re) 및 커스텀 로직을 통한 AI 응답 필터링.

---

## 3. 현 프로젝트 적용 사례 (코드 및 전략)

### 3.1 페르소나 기반 프롬프트 엔지니어링 (tasks/question_generator.py)
단순한 질문 생성이 아닌, **"베테랑 면접관"**의 페르소나를 부여하고 엄격한 출력 규칙을 정의하여 일관된 톤앤매너를 유지한다.

```python
# [프롬프트 설계 사례]
PROMPT_TEMPLATE = """[|user|]당신은 전문적인 지식과 공정한 태도를 겸비한 베테랑 AI 면접관입니다.
다음 지침에 따라 지원자의 잠재력을 예리하게 파악할 수 있는 **단 하나의 질문**을 생성하십시오.

### [면접 전략]
- 평가 대상 직무: {target_role}
- 면접 단계: {stage_name} ({guide})

### [참고 문맥]
{context}

### [출력 규칙]
1. 인사말, 부연 설명을 절대 하지 마십시오.
2. 오직 물음표(?)로 끝나는 단일 문장의 질문만 출력하십시오.
...
[|assistant|]"""
```

### 3.2 환각(Hallucination) 방지 및 예외 처리
지원자가 무의미한 답변(자음 나열, "모름" 등)을 할 경우, AI가 잘못된 추측을 하지 않도록 방어 로직을 구축했다.

```python
# [무의미한 답변 체크 로직]
def is_meaningless(text: str) -> bool:
    if len(text) < 5: return True
    if re.fullmatch(r'[ㄱ-ㅎㅏ-ㅣ\s]+', text): return True # 'ㅋㅋㅋㅋ' 등 필터링
    return False

# [전략적 컨텍스트 차단]
if is_meaningless(last_user_transcript.text):
     context_text = "[주의: 지원자의 이전 답변이 무의미합니다. 과거 정보에 의존하지 말고 다시 요청하십시오.]"
```

### 3.3 단계별 맞춤형 질문 전략 (Behavioral vs Technical)
면접 단계(Stage)가 인성 질문(Narrative)인지 기술 질문인지에 따라 LLM에 내리는 지시어(Instruction)를 동적으로 변경한다.

*   **기술 질문**: "기술 스택과 수치를 적절히 인용하여 예리한 꼬리질문을 하십시오."
*   **인성 질문**: "**코드/설계 단어를 배제하고**, 태도와 가치관을 확인하는 데 집중하십시오."

---

## 4. 종합 비교 및 분석

| 항목 | 일반적인 챗봇 서비스 | 우리 프로젝트 (AI 면접관) |
| :--- | :--- | :--- |
| **언어 모델** | 일반 GPT 계열 | 한국어 특화 **EXAONE 3.5** |
| **문맥 참조** | 단순 대화 히스토리 | **RAG(이력서 DB) + 대화 히스토리** |
| **응답 정제** | AI가 주는 대로 출력 | **정규표현식 기반 5단계 정제** |
| **동작 방식** | 사용자 주도 (QA) | AI 주도 (인터뷰 시나리오 기반) |

---

## 5. 결론 및 성과
이러한 NLP 기술의 결합을 통해, 우리 시스템은 단순히 준비된 질문을 던지는 것이 아니라 **지원자의 이력서 맥락을 완벽히 이해하고 압박 면접까지 수행 가능한** 지능형 면접관을 구현하였다.

---

**[작성 위치]**
*   파일 경로: `C:\big20\Big20_aI_interview_project\파이널_진행보고서\프롬프트엔지니어링및라그\01.RAG-LLM-NLP-적용사례.md`
