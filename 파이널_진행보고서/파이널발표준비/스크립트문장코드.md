# BIGTERVIEW AI-Worker 발표 스크립트 기반 코드 매핑 분석 리포트

본 문서는 '빅터뷰(BIGTERVIEW)' 프로젝트의 AI-Worker 파이프라인 발표 스크립트 각 문장이 실제 코드의 어떤 부분과 대응되는지 상세히 설명합니다.

---

## 1. 배경 (Background) - 40초

| 스크립트 문장                                                  | 관련 코드 위치 및 설명                                                                                                                                                                                                         |
| :------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "지원자의 이력서와 자기소개서를 깊이 있게 분석하여..."         | **`tasks/parse_resume.py`**`<br>pdfplumber`를 활용해 PDF 내의 텍스트뿐만 아니라 표(Table) 구조까지 정밀하게 파싱하여 `header`, `education`, `activities`, `projects`, `self_intro` 등으로 구조화합니다.    |
| "실제 면접관처럼 개인 맞춤형 꼬리질문을 자동 생성하고..."      | **`tasks/question_generator.py`** (Line 312-402)`<br>retrieve_context`를 통해 지원자의 이력서에서 현재 질문 단계와 가장 관련성이 높은 청크를 검색(RAG)하고, 이를 LLM 프롬프트에 주입하여 개인화된 질문을 생성합니다. |
| "전문가 수준의 심층 평가 리포트까지 제공하는 AI 파이프라인..." | **`tasks/evaluator.py`** (Line 370-401)`<br>FinalReportSchema`를 정의하고 'AI 채용 평가 위원회' 페르소나를 부여해 기술력, 성격, 조직 적합성을 수치화하고 정성적 피드백을 생성합니다.                                 |

---

## 2. 설계 (Design) - 55초

| 스크립트 문장                                                                          | 관련 코드 위치 및 설명                                                                                                                                                      |
| :------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "각 처리 단계에 최적화된 모듈을 Celery 비동기 큐로 연결한 마이크로서비스 지향 구조..." | **`main.py`** (Line 36-84)`<br>`Celery 앱을 설정하고 `task_routes`를 통해 `cpu_queue`와 `gpu_queue`를 분리하여 태스크 성격에 맞는 워커로 라우팅합니다.      |
| "비동기로 이력서를 구조화하고 벡터 DB에 저장하는 오프라인 단계..."                     | **`tasks/resume_embedding.py`** (Line 15-81)`<br>generate_resume_embeddings` 태스크가 비동기로 실행되어 PDF 파싱 -> 청킹 -> 임베딩 -> PGVector 저장을 수행합니다. |
| "RAG 검색을 통해 실시간으로 질문을 생성하는 온라인 단계..."                            | **`tasks/question_generator.py`** (Line 86-97)`<br>`인터뷰 진행 중 `generate_next_question_task`가 호출되어 실시간으로 문맥을 검색하고 질문을 생성합니다.       |
| "표 인식률이 압도적인 pdfplumber를 채택..."                                            | **`tasks/parse_resume.py`** (Line 1, 55-63)`<br>import pdfplumber` 후 `page.extract_tables()`를 사용하여 복잡한 이력서 양식의 표 데이터를 유실 없이 추출합니다. |
| "임베딩에는 이력서 특유의 어체 이해도가 높은 KURE-v1 모델..."                          | **`tasks/embedding.py`** (Line 11)`<br>EMBEDDING_MODEL = "nlpai-lab/KURE-v1"`을 사용하여 한국어 문장 간 의미 유사도를 극대화합니다.                               |
| "매인 LLM으로는 엑사원(EXAONE) 3.5 7.8B 모델을 선정..."                                | **`tasks/question_generator.py`** (Line 23-25)`<br>EXAONE-3.5-7.8B-Instruct-Q4_K_M.gguf` 로컬 경로를 설정하고 싱글톤 패턴으로 로드하여 사용합니다.                |

---

## 3. 구현 (Implementation) - 55초

| 스크립트 문장                                                              | 관련 코드 위치 및 설명                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| :------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "문서를 의미 경계로 청킹하여 PGVector에 저장..."                           | **`tasks/chunking.py`** 및 **`tasks/pgvector_store.py`**`<br>`이력서의 각 섹션별로 의미가 끊기지 않도록 청킹하고, `store_embeddings` 함수를 통해 PGVector 스키마에 맞춰 벡터 데이터를 저장합니다.                                                                                                                                                                                                                                                                |
| "면접 흐름은 일반 직무형과 전환자형으로 이원화하고, 총 15단계 시나리오..." | **`tasks/question_generator.py`** (Line 136-171)`<br>check_if_transition(major, position)` 함수로 전환자 여부를 판별하고, `get_next_stage_normal`/`get_next_stage_transition`을 호출해 15개 스테이지를 순차 진행합니다.                                                                                                                                                                                                                                                |
| "6레이어 다층 방어형 프롬프트 아키텍처"                                    | **`tasks/question_generator.py`** 전체`<br>`1. **입력 유효성 검증**: `is_meaningless` (Line 27)`<br>`2. **페르소나 주입**: `PROMPT_TEMPLATE` (Line 44)`<br>`3. **동적 지시어**: `mode_instruction`, `global_constraint` (Line 439-502)`<br>`4. **출력 정제 (Regex)**: `meta_patterns` 등 (Line 509-584)`<br>`5. **문장 부호 정제**: (Line 690-704)`<br>`6. **안전망 (Fallback)**: `fallback_dict` (Line 751-765) |

---

## 4. 문제 및 해결 (Problem & Solution) - 140초

### 4.1 인프라 및 아키텍처 (Infrastructure)

| 스크립트 문장                                        | 관련 코드 위치 및 설명                                                                                                                                                                                          |
| :--------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "평가는 면접 종료 후 배치로 일괄 처리하도록 분리..." | **`tasks/evaluator.py`** (Line 234-274)`<br>generate_final_report`가 개별 답변 평가(`analyze_answer`)를 `group`으로 묶어 병렬 처리하고, 최종 리포트를 생성하는 '전략적 지연 평가'를 구현했습니다. |
| "명시적 큐 분리와 네이밍 컨벤션..."                  | **`main.py`** (Line 64-83)`<br>gpu_queue`와 `cpu_queue`를 명확히 나누어, 질문 생성 워커가 평가 워커에 의해 자원을 선점당하지 않도록 격리했습니다.                                                   |

### 4.2 LLM 및 프롬프트 제어 (Prompt Control)

| 스크립트 문장                                                     | 관련 코드 위치 및 설명                                                                                                                                                                                                        |
| :---------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "AI가 질문 대신 의도를 설명하는 메타 발화 오류..."                | **`tasks/question_generator.py`** (Line 509-518)`<br>meta_patterns` 정규식을 통해 "이 질문은 ~를 확인하기 위함입니다"와 같은 AI의 사족을 강제로 제거합니다.                                                         |
| "단계별 미션을 동적으로 주입하는 3계층 변수형 프롬프트 시스템..." | **`tasks/question_generator.py`** (Line 439-466)`<br>mode_task_instruction`, `mode_instruction`, `global_constraint` 변수를 단계별(skill, communication, responsibility 등)로 다르게 생성하여 LLM에 주입합니다. |

### 4.3 DB 및 API 연동 (API & Sync)

| 스크립트 문장                                              | 관련 코드 위치 및 설명                                                                                                                                                                                                 |
| :--------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "AbortController를 도입해 불필요한 API 호출 절감..."       | **Frontend (React)** (스크립트 언급 내용)`<br>`사용자가 답변을 마칠 때 이전의 폴링이나 대기 중인 요청을 취소하여 서버 부하를 방지합니다.                                                                       |
| "DB 상태값이 완전히 업데이트된 이후에만 다음 큐를 발행..." | **`tasks/question_generator.py`** (Line 181-195)`<br>last_user_transcript.question_id != last_ai_transcript.question_id` 조건 등을 체크하여 경합 조건(Race Condition)을 방지하고 상태 기반으로 동기화합니다. |

---

## 5. 결과 및 느낀점 (Results & Feedback) - 70초

| 스크립트 문장                                                          | 관련 코드 위치 및 설명                                                                                                                                                                  |
| :--------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "초기 단계에서 발생하던 8대 주요 시스템 및 로직 오류를 100% 안정화..." | `question_generator.py` 내의 수많은 `if/else` 분기 처리와 `fallback` 로직, 그리고 `evaluator.py`의 예외 처리 로직들이 이 안정성을 담보합니다.                                   |
| "모듈 간 결합에서 발생하는 데이터 정합성 문제나 큐 네이밍 충돌..."     | `main.py`의 경로 우선순위 조정(Line 6-21) 및 명시적 `task_routes` 설정이 협업 과정에서의 시행착오를 해결한 결과물입니다.                                                            |
| "구조를 합의하고 기준을 공유하는 협업의 중요성..."                     | **`tasks/evaluator.py`** (Line 81-108)`<br>AnswerEvalSchema`, `FinalReportSchema`와 같은 Pydantic 모델을 정의하여 데이터 인터페이스 명세를 사전에 합의한 구조를 보여줍니다. |
