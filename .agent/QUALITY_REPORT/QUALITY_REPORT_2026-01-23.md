---
description: 프로젝트 배포 전/후 시스템의 품질 및 안정성을 검사하는 절차
---

# 🛡️ 시스템 품질 검사 (Quality Check) 워크플로우

이 워크플로우는 PostgreSQL 18 업데이트 및 대규모 스키마 변경 이후 시스템의 각 구성 요소가 정상적으로 작동하는지 확인하기 위한 절차입니다.

## 1. 데이터베이스 연결성 및 드라이버 점검
// turbo
1. 컨테이너 내부 네트워크 호스트 이름 확인:
   - 모든 연결 문자열이 `localhost`가 아닌 `db`로 설정되어 있는지 확인합니다.
   - `psycopg` (v3) 드라이버 사용 여부를 체크합니다. (`postgresql+psycopg://...`)

// turbo
2. DB 스키마 일관성 검사:
   - `backend-core/models.py`와 `ai-worker/db.py`의 테이블 정의가 일치하는지 파일 비교를 수행합니다.

## 2. Docker 인프라 상태 점검
// turbo
1. 서비스 상태 확인:
   ```bash
   docker-compose ps
   ```
   - 모든 서비스(db, redis, backend, ai-worker, media-server, frontend)가 `Up` 상태인지 확인합니다.

// turbo
2. DB 초기화 로그 확인:
   ```bash
   docker logs interview_db | grep "initialized successfully"
   ```
   - `init.sql`이 정상적으로 실행되어 테이블과 인덱스가 생성되었는지 확인합니다.

## 3. AI 워커 및 모델 상태 점검
// turbo
1. Solar 모델 로드 확인:
   ```bash
   docker logs interview_worker | grep "Solar-10.7B model loaded successfully"
   ```
   - 모델 가중치가 RAM에 정상적으로 로드되었는지 확인합니다.

2. Celery 태스크 처리 여부 확인:
   - 답변 제출 시 `tasks.evaluator.analyze_answer` 로그가 찍히는지 관찰합니다.

## 4. End-to-End 통합 테스트
1. **인증 테스트**: 회원가입 및 로그인 절차가 정상적으로 수행되어 토큰이 발급되는지 확인합니다.
2. **면접 세션 생성**: 직무 입력 후 `interviews` 테이블에 레코드가 생성되고 질문이 자동 생성되는지 확인합니다.
3. **STT 및 대화 기록**: 음성 입력 시 `transcripts` 테이블에 실시간으로 데이터가 쌓이는지 확인합니다.
4. **최종 리포트**: 면접 종료 후 10~20초 내에 종합 평가 리포트가 생성되어 화면에 출력되는지 확인합니다.

## 5. 성능 및 리소스 점검
// turbo
1. GPU/CPU 점유율 확인:
   - `ai-worker`가 Solar 모델 추론 시 CPU 스레드를 적절히 사용하는지 모니터링합니다.
   - `backend-core`가 Llama-3.2 모델 가속을 위해 VRAM을 점유하고 있는지 확인합니다.

---

### 품질 검사 통과 기준 (Definition of Done)
- [ ] 모든 API 엔드포인트가 HTTP 200/201을 반환함
- [ ] DB 테이블 간 Foreign Key 제약 조건이 깨지지 않음
- [ ] AI 리포트 생성 시간이 평균 30초 이내임
- [ ] WebSocket(STT) 연결이 면접 도중 끊기지 않음
