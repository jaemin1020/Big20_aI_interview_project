# AI 모의면접 평가 시스템 - 실제 루브릭 적용 완료

## 📋 개요

실제 AI 모의면접 평가 루브릭(평가루브릭.md)을 기반으로 시스템을 재구성했습니다.

---

## 🎯 평가 영역 (A~E)

### A. 자기 표현 & 기본 커뮤니케이션 (15%)

**대상 단계**: 자기소개, 최종 자유 발언

**평가 기준**:

- 본인 배경과 강점이 명확히 전달되는가
- 답변 구조가 이해하기 쉬운가
- 질문 의도에서 벗어나지 않는가

**LLM 관찰 포인트**:

- 서론–본론–결론 구조 존재 여부
- 핵심 키워드 반복 및 강조 여부

**감점 요인**:

- 장황하지만 핵심이 없음
- 질문과 무관한 이야기 반복

---

### B. 지원 동기 & 회사 적합성 (15%)

**대상 단계**: 지원 동기

**평가 기준**:

- 지원 직무를 정확히 이해하고 있는가
- 회사/도메인 언급이 구체적인가
- 단순 열정이 아닌 명확한 선택 이유가 있는가

**LLM 관찰 포인트**:

- 회사 인재상 키워드와의 정렬 여부
- 이력서 프로젝트·경험과 동기의 연결성

**감점 요인**:

- 어디든 쓸 수 있는 범용 동기
- 회사명만 바꿔도 성립하는 답변

---

### C. 직무 지식 이해도 (20%)

**대상 단계**: 직무 관련 지식 질문, 추가 질문 (1-1)

**평가 기준**:

- 기본 개념을 정확히 설명했는가
- 용어를 맥락에 맞게 사용했는가
- **추가 질문 후 설명이 개선되었는가** ⭐

**LLM 관찰 포인트**:

- 개념 오류 여부
- 재질문 후 답변 명확성 변화

**감점 요인**:

- 용어 나열만 하고 설명 불가
- 재질문 후에도 동일한 모호한 답변

**추가 질문 평가**:

- 개선 확인 시: +10점 가산
- 개선 없음: -5점 감점

---

### D. 직무 경험 & 문제 해결 (30%) ⭐ 최고 가중치

**대상 단계**: 직무 관련 경험 질문, 문제 해결 질문, 추가 질문 (2-1, 3-1)

**평가 기준**:

- 실제 경험 기반 설명인가
- 문제 정의 → 접근 → 결과 흐름이 있는가
- 대안·개선 관점이 있는가
- **추가 질문 후 논리 보완이 되었는가** ⭐

**LLM 관찰 포인트**:

- "내가 했다" 중심 서술 여부
- 추상적 설명 vs 구체적 행동

**감점 요인**:

- 팀이 했다는 이야기만 반복
- 결과 없는 과정 설명

**추가 질문 평가**:

- 개선 확인 시: +15점 가산
- 개선 없음: -10점 감점

---

### E. 인성 & 성장 가능성 (20%)

**대상 단계**: 협업 평가 (+1-1), 책임감·가치관 (+2-1), 변화 수용·성장 (+3-1)

**평가 기준**:

- 협업 시 태도가 성숙한가
- 책임 회피 없이 설명하는가
- 실패·변화를 학습 관점으로 해석하는가
- **추가 질문 후 태도 설명이 명확해졌는가** ⭐

**LLM 관찰 포인트**:

- **blame language vs ownership language**
- "배웠다 / 개선했다" 표현 사용 여부

**감점 요인**:

- 타인·환경 탓 중심 설명
- 실패 경험 회피

**추가 질문 평가**:

- 개선 확인 시: +10점 가산
- 개선 없음: -5점 감점

---

## 💻 코드 구현

### 1. 평가 루브릭 생성 (`backend-core/utils/rubric_generator.py`)

```python
from utils.rubric_generator import create_evaluation_rubric

# 전체 평가 루브릭 생성
rubric = create_evaluation_rubric()

# 개별 영역 루브릭
from utils.rubric_generator import (
    create_area_a_rubric,  # 자기 표현
    create_area_b_rubric,  # 지원 동기
    create_area_c_rubric,  # 직무 지식
    create_area_d_rubric,  # 직무 경험
    create_area_e_rubric,  # 인성
)
```

### 2. 질문 생성 헬퍼 (`backend-core/utils/question_helper.py`)

```python
from utils.question_helper import create_question_with_rubric, QuestionType

# 자기소개 질문 생성
q1 = create_question_with_rubric(
    content="간단히 자기소개 부탁드립니다.",
    question_type=QuestionType.SELF_INTRODUCTION,
    category="BEHAVIORAL",
    difficulty="EASY"
)

# 직무 지식 질문 생성
q2 = create_question_with_rubric(
    content="LangChain의 RAG 시스템에 대해 설명해주세요.",
    question_type=QuestionType.KNOWLEDGE,
    category="TECHNICAL",
    difficulty="MEDIUM"
)

# 추가 질문 (1-1) 생성
q3 = create_question_with_rubric(
    content="조금 더 구체적으로 설명해주시겠어요?",
    question_type=QuestionType.KNOWLEDGE,
    category="TECHNICAL",
    difficulty="MEDIUM",
    is_follow_up=True  # 추가 질문 표시
)
```

### 3. Question 모델 업데이트

```python
class Question(SQLModel, table=True):
    # ... 기존 필드
  
    # 새로 추가된 필드
    question_type: Optional[str]  # "자기소개", "지원동기", "직무지식" 등
    is_follow_up: bool = False    # 추가 질문 여부
    parent_question_id: Optional[int]  # 원 질문 ID (추가 질문인 경우)
  
    rubric_json: Dict[str, Any]  # 평가 루브릭 (A~E 중 하나)
```

---

## 📊 평가 출력 형식

```json
{
  "A_자기표현_커뮤니케이션": {
    "score": 85,
    "feedback": "서론-본론-결론 구조가 명확하고 핵심이 잘 전달됨",
    "weight": 0.15
  },
  "B_지원동기_회사적합성": {
    "score": 78,
    "feedback": "회사 인재상과 연결은 있으나 다소 일반적",
    "weight": 0.15
  },
  "C_직무지식_이해도": {
    "score": 82,
    "feedback": "기본 개념 이해 양호, 추가 질문 후 설명 개선됨 (+10점)",
    "weight": 0.20,
    "follow_up_bonus": 10
  },
  "D_직무경험_문제해결": {
    "score": 88,
    "feedback": "구체적 경험 기반 설명, 문제 정의→접근→결과 흐름 명확, 추가 질문 후 논리 보완됨 (+15점)",
    "weight": 0.30,
    "follow_up_bonus": 15
  },
  "E_인성_성장가능성": {
    "score": 80,
    "feedback": "ownership language 사용, 학습 관점 해석 양호",
    "weight": 0.20
  },
  "total_score": 83.35,
  "weighted_score": 83.35,
  "pass_probability": "High",
  "summary": "전반적으로 우수한 답변. 특히 직무 경험 설명이 구체적이고 문제 해결 과정이 명확함. 추가 질문에 대한 개선도 확인됨."
}
```

---

## 🔄 추가 질문 평가 로직

### C, D, E 영역에서 추가 질문 평가

```python
def evaluate_with_follow_up(
    initial_answer: str,
    follow_up_answer: str,
    rubric: Dict
) -> Dict:
    """
    추가 질문 평가 로직
  
    Returns:
        {
            "initial_score": 70,
            "follow_up_score": 85,
            "improvement": 15,
            "bonus": 10,  # 개선 시 가산점
            "final_score": 85
        }
    """
    # 1. 초기 답변 평가
    initial_score = evaluate_answer(initial_answer, rubric)
  
    # 2. 추가 질문 답변 평가
    follow_up_score = evaluate_answer(follow_up_answer, rubric)
  
    # 3. 개선 여부 판단
    improvement = follow_up_score - initial_score
  
    # 4. 가산/감점 적용
    if improvement > 5:
        bonus = rubric["follow_up_evaluation"]["improvement_bonus"]
    elif improvement < -5:
        bonus = rubric["follow_up_evaluation"]["no_change_penalty"]
    else:
        bonus = 0
  
    final_score = min(100, follow_up_score + bonus)
  
    return {
        "initial_score": initial_score,
        "follow_up_score": follow_up_score,
        "improvement": improvement,
        "bonus": bonus,
        "final_score": final_score
    }
```

---

## 📝 LLM 평가 프롬프트

```python
from utils.rubric_generator import get_evaluation_prompt

prompt = get_evaluation_prompt()
# 출력:
# 너는 IT 기업의 신입 채용 면접관이다.
# 아래 제공되는 이력서 요약, 회사 인재상, 면접 질문과 지원자 답변을 바탕으로
# 지원자의 면접 답변을 평가하라.
# 
# [평가 원칙]
# - 평가는 아래 A~E 기준에 따라 수행한다.
# - 추가 질문(1-1, 2-1, 3-1)이 존재하는 경우,
#   초기 답변 대비 설명의 명확성·논리성·태도 개선 여부를 반드시 비교 평가하라.
#   개선이 확인되면 가산, 동일하거나 악화되면 감산하라.
# ...
```

---

## 🎯 사용 시나리오

### 시나리오 1: 질문 생성 시 루브릭 자동 적용

```python
# 면접 질문 생성
questions = [
    create_question_with_rubric("자기소개 부탁드립니다.", QuestionType.SELF_INTRODUCTION),
    create_question_with_rubric("지원 동기를 말씀해주세요.", QuestionType.MOTIVATION),
    create_question_with_rubric("LangChain이란?", QuestionType.KNOWLEDGE),
    create_question_with_rubric("조금 더 구체적으로?", QuestionType.KNOWLEDGE, is_follow_up=True),
]

# DB 저장
for q_data in questions:
    question = Question(**q_data)
    session.add(question)
session.commit()
```

### 시나리오 2: 평가 시 루브릭 활용

```python
# 답변 평가
answer = "LangChain은 LLM 애플리케이션 개발 프레임워크입니다..."

# 질문의 루브릭 가져오기
question = session.get(Question, question_id)
rubric = question.rubric_json

# LLM 평가
evaluation = llm.evaluate(
    answer=answer,
    rubric=rubric,
    prompt=get_evaluation_prompt()
)

# 추가 질문이 있는 경우
if question.is_follow_up:
    # 원 질문 답변과 비교
    parent_answer = get_parent_answer(question.parent_question_id)
    evaluation_with_bonus = evaluate_with_follow_up(
        parent_answer, answer, rubric
    )
```

---

## ✅ 완료된 작업

1. ✅ **평가 루브릭 생성 도구** (`rubric_generator.py`)

   - A~E 5개 영역 루브릭 구현
   - 추가 질문 평가 로직 포함
   - LLM 평가 프롬프트 생성
2. ✅ **질문 생성 헬퍼** (`question_helper.py`)

   - 질문 유형별 루브릭 자동 매핑
   - 추가 질문 표시 기능
3. ✅ **Question 모델 업데이트**

   - `question_type` 필드 추가
   - `is_follow_up` 필드 추가
   - `parent_question_id` 필드 추가
4. ✅ **문서화**

   - 평가 영역별 상세 기준
   - 코드 사용 예시
   - 평가 출력 형식

---

## 🚀 다음 단계

1. **평가 엔진 구현**

   - LLM 기반 자동 평가
   - 추가 질문 개선도 비교
   - 가산/감점 자동 적용
2. **면접 시나리오 구성**

   - 단계별 질문 순서 정의
   - 추가 질문 트리거 조건
3. **평가 리포트 생성**

   - A~E 영역별 점수 시각화
   - 합격 가능성 판단
   - 개선 피드백 제공

---

**작성일**: 2026-01-29
**버전**: 3.0 (실제 루브릭 반영)
